if type -q exa
    alias ll "exa --long --group --icons"
    alias la "ll --all"
    alias tree "exa --tree --icons"
end

if type -q bpytop
    alias top bpytop
end

alias g git
alias n nvim
alias t tmux
alias d docker
alias dc docker-compose
